<%= form_for(@bid, html: {role: "form", class: "form-inline"}) do |f| %>
  <% if @bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

      <ul>
      <% @bid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-xs-2">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>
  <div class="form-group col-xs-2">
    <%= @bid.item.code %> <%= @bid.item.name %>
  </div>
  <div class="form-group col-xs-2">
    <%= @bid.user.title %> <%= @bid.user.first_name %> <%= @bid.user.last_name %>
  </div>

  <div class="actions form-group col-xs-2">
    <%= f.submit :id => 'bid_update', :class => 'form-control btn btn-primary' %>
  </div>
<% end %>
