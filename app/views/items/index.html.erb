<div id="items" class="container">
  <% @items.each do |item| %>
    <div class="col-md-12">
      <%= link_to item.name, user_item_path(user_id: params[:user_id], id: item.id) %>
    </div>
  <% end %>
</div>
