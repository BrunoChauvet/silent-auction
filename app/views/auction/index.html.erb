<div id="auction" class="auction" ng-controller="AuctionCtrl" ng-init="init()">
  <div class="auction-items">
    <div class="header col-md">
      <div class="auction-title">
        <%= image_tag "ufe_logo.png", :class => 'logo' %>
        UFE Charity Gala 2013
      </div>
      <div class="auction-info">
        <div>
          <span class="label">Amount raised</span>
          <span class="value">{{amountRaised | currency}}</span>
        </div>
        <div>
          <span class="label">Time left</span>
          <span class="value">{{timer}}</span>
        </div>
      </div>
    </div>

    <div ng-repeat="item in pageItems" class="item-container {{item.item.category}} col-md">
    	<div class="item-image col-md">
    		<img ng-src="{{item.item.image}}"/>
    	</div>
      
    	<div class="item-content col-md">
        <div class="item-detail col-md">
    	  	<span class="item-code">{{item.item.code}}</span>
          <span class="item-name">{{item.item.name}}</span>
        </div>
  	  	<div class="item-bid col-md">
          <div ng-show="item.bid.price" class="col-md item-user">{{item.user.title}} {{item.user.first_name}} {{item.user.last_name}}</div>
          <div ng-hide="item.bid.price" class="col-md item-user">Start price</div>
  		  	<div class="col-md item-price">
            <span ng-show="item.bid.price">{{item.bid.price | currency}}</span>
            <span ng-hide="item.bid.price">{{item.item.start_price | currency}}</span>
          </div>
  		  </div>
    	</div>
    </div>
  </div>

  <button type="submit" class="btn btn-default" ng-click="loadNextPage()">
    <span class=".glyphicon .glyphicon-chevron-right">Next page</span>
  </button>
</div>
