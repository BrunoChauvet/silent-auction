<%= form_for(@user_group, html: {role: "form", class: "form-inline"}) do |f| %>
  <% if @user_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_group.errors.count, "error") %> prohibited this user_group from being saved:</h2>

      <ul>
      <% @user_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-xs-2">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="form-group col-xs-1">
    <%= f.label :sort_order %>
    <%= f.text_field :sort_order %>
  </div>

  <div class="actions form-group col-xs-2">
    <%= f.label :update %>
    <%= f.submit :id => 'user_group_update', :class => 'form-control btn btn-primary' %>
  </div>
<% end %>
